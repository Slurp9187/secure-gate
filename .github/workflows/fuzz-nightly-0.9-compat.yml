name: Fuzz (nightly – 0.9 compat)

on:
  schedule:
    - cron: "0 4 * * *" # 04:00 UTC daily
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "secure-gate-compat/fuzz/**"
      - "secure-gate-compat/src/**"
      - "secure-gate-compat/Cargo.toml"
  pull_request:
    paths:
      - "secure-gate-compat/fuzz/**"
      - "secure-gate-compat/src/**"
      - "secure-gate-compat/Cargo.toml"

permissions:
  contents: read

jobs:
  fuzz:
    name: ${{ matrix.target }} (${{ matrix.mode }})
    runs-on: ubuntu-latest
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        target: [compat_v08, compat_v10]
        mode: [normal, asan]

    env:
      # NOTE: do not add `-runs=0`. In libFuzzer fork mode (`-fork=1`) the
      # parent's run budget is checked against zero immediately, causing the
      # whole fuzz session to exit in ~2s regardless of -max_total_time.
      # Omit -runs entirely; the default (-1, infinite) plus -max_total_time
      # gives the intended ~50-minute wall-clock budget.
      FUZZER_ARGS: >-
        -fork=1
        -timeout=30
        -rss_limit_mb=4096
        -max_total_time=3000
        -max_len=4096
        -use_value_profile=1
        -detect_leaks=0

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Rust nightly + rust-src
        uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: nightly
          components: rust-src

      - name: Cache cargo registry + target
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: "secure-gate-compat/fuzz"

      - name: Install cargo-fuzz
        # Do not pass `--locked`: cargo-fuzz 0.13.1's bundled lockfile pins
        # rustix 0.36.5, which fails to build on current nightly rustc with
        # "attributes starting with `rustc` are reserved". Fresh resolution
        # picks a newer rustix that compiles cleanly.
        run: cargo install cargo-fuzz

      - name: Fuzz – ${{ matrix.target }} (${{ matrix.mode }})
        env:
          RUSTFLAGS: ${{ matrix.mode == 'asan' && '-Zsanitizer=address -Zsanitizer=leak -C debuginfo=2' || '' }}
          ASAN_OPTIONS: "detect_leaks=1:abort_on_error=1:symbolize=1:allocator_may_return_null=1"
        run: |
          echo "Fuzzing ${{ matrix.target }} (${{ matrix.mode }})..."
          cd secure-gate-compat && cargo +nightly fuzz run \
            ${{ matrix.target }} \
            -- \
            ${{ env.FUZZER_ARGS }}

      - name: Upload crash artifacts (if any)
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: fuzz-crash-nightly-${{ matrix.target }}-${{ matrix.mode }}-${{ github.run_id }}
          path: secure-gate-compat/fuzz/artifacts/${{ matrix.target }}/**
          retention-days: 14

      - name: Upload corpus (best effort)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: fuzz-corpus-nightly-${{ matrix.target }}-${{ matrix.mode }}-${{ github.run_id }}
          path: secure-gate-compat/fuzz/corpus/${{ matrix.target }}/
          retention-days: 30
          if-no-files-found: warn
