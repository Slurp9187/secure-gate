// Add this block — tests bincode on SecurePassword directly
if let Ok(pw) = bincode::decode_from_slice::<SecurePassword, _>(data, config) {
    let (pw, _) = pw;
    let _ = pw.expose().len();
    drop(pw);
}

let pw = SecurePassword::from("fuzzme");
if let Ok(encoded) = bincode::encode_to_vec(&pw, config) {
    let _ = bincode::decode_from_slice::<SecurePassword, _>(&encoded, config);
}