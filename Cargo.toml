[package]
name = "secure-types"
version = "0.1.0"
edition = "2021"
description = "Zero-overhead, feature-gated secure wrappers for secrets"
license = "MIT OR Apache-2.0"
keywords = ["crypto", "security", "zeroize", "secrets", "serde", "no-std"]
categories = ["cryptography", "no-std"]

[dependencies]
secrecy = { version = "0.10.3", optional = true, default-features = false, features = [
  "alloc",
] }
serde = { version = "1.0", features = ["derive"], optional = true }

[dev-dependencies]
serde_json = "1.0"

[features]
default = ["zeroize"]
zeroize = ["secrecy"]
serde = ["dep:serde"]
serde-zeroize = ["serde", "zeroize"]
