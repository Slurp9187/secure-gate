error[E0277]: `Vec<u8>` owns a heap allocation, so it cannot be the inner type of a `Fixed`
  --> tests/compile-fail/fixed_reallocating_inner.rs:19:1
   |
19 | fixed_newtype!(pub LeakyBytes, generic Vec<u8>);
   | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
   | |
   | `Fixed` holds its secret inline; `Vec<u8>` does not qualify
   | required by a bound introduced by this call
   |
   = help: the trait `FixedStorage` is not implemented for `Vec<u8>`
   = note: `Fixed<T>` exists so a secret can be held with no allocator at all, and `FixedStorage` is what keeps that true: a heap allocation can be abandoned unwiped, either by a reallocation or by replacing the whole value.
   = note: For a heap-backed secret use `Dynamic<T>` instead, which documents the residue and offers one safe growth path. `Dynamic` needs this crate's `alloc` feature — if you are seeing this with `alloc` off while still able to name `Vec` or `String`, enabling it is the fix.
   = note: For a custom inner type that genuinely owns no heap allocation, write an empty `impl FixedStorage` block for it. That is an assertion the compiler cannot check, so only write it if it is true.
   = help: the following other types implement trait `FixedStorage`:
             ()
             (A, B)
             (A, B, C)
             (A, B, C, D)
             (A,)
             Option<T>
             [T; N]
             bool
           and $N others
note: required by a bound in `Fixed::<T>::new`
  --> src/fixed.rs
   |
   |     pub const fn new(value: T) -> Self
   |                  --- required by a bound in this associated function
   |     where
   |         T: crate::FixedStorage,
   |            ^^^^^^^^^^^^^^^^^^^ required by this bound in `Fixed::<T>::new`
   = note: this error originates in the macro `fixed_newtype` (in Nightly builds, run with -Z macro-backtrace for more info)

error[E0277]: `String` owns a heap allocation, so it cannot be the inner type of a `Fixed`
  --> tests/compile-fail/fixed_reallocating_inner.rs:20:1
   |
20 | fixed_newtype!(pub LeakyText, generic String);
   | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
   | |
   | `Fixed` holds its secret inline; `String` does not qualify
   | required by a bound introduced by this call
   |
   = help: the trait `FixedStorage` is not implemented for `String`
   = note: `Fixed<T>` exists so a secret can be held with no allocator at all, and `FixedStorage` is what keeps that true: a heap allocation can be abandoned unwiped, either by a reallocation or by replacing the whole value.
   = note: For a heap-backed secret use `Dynamic<T>` instead, which documents the residue and offers one safe growth path. `Dynamic` needs this crate's `alloc` feature — if you are seeing this with `alloc` off while still able to name `Vec` or `String`, enabling it is the fix.
   = note: For a custom inner type that genuinely owns no heap allocation, write an empty `impl FixedStorage` block for it. That is an assertion the compiler cannot check, so only write it if it is true.
   = help: the following other types implement trait `FixedStorage`:
             ()
             (A, B)
             (A, B, C)
             (A, B, C, D)
             (A,)
             Option<T>
             [T; N]
             bool
           and $N others
note: required by a bound in `Fixed::<T>::new`
  --> src/fixed.rs
   |
   |     pub const fn new(value: T) -> Self
   |                  --- required by a bound in this associated function
   |     where
   |         T: crate::FixedStorage,
   |            ^^^^^^^^^^^^^^^^^^^ required by this bound in `Fixed::<T>::new`
   = note: this error originates in the macro `fixed_newtype` (in Nightly builds, run with -Z macro-backtrace for more info)

error[E0277]: `Vec<u8>` owns a heap allocation, so it cannot be the inner type of a `Fixed`
  --> tests/compile-fail/fixed_reallocating_inner.rs:23:24
   |
23 |     let _ = Fixed::new(vec![1u8, 2, 3]);
   |             ---------- ^^^^^^^^^^^^^^^ `Fixed` holds its secret inline; `Vec<u8>` does not qualify
   |             |
   |             required by a bound introduced by this call
   |
   = help: the trait `FixedStorage` is not implemented for `Vec<u8>`
   = note: `Fixed<T>` exists so a secret can be held with no allocator at all, and `FixedStorage` is what keeps that true: a heap allocation can be abandoned unwiped, either by a reallocation or by replacing the whole value.
   = note: For a heap-backed secret use `Dynamic<T>` instead, which documents the residue and offers one safe growth path. `Dynamic` needs this crate's `alloc` feature — if you are seeing this with `alloc` off while still able to name `Vec` or `String`, enabling it is the fix.
   = note: For a custom inner type that genuinely owns no heap allocation, write an empty `impl FixedStorage` block for it. That is an assertion the compiler cannot check, so only write it if it is true.
   = help: the following other types implement trait `FixedStorage`:
             ()
             (A, B)
             (A, B, C)
             (A, B, C, D)
             (A,)
             Option<T>
             [T; N]
             bool
           and $N others
note: required by a bound in `Fixed::<T>::new`
  --> src/fixed.rs
   |
   |     pub const fn new(value: T) -> Self
   |                  --- required by a bound in this associated function
   |     where
   |         T: crate::FixedStorage,
   |            ^^^^^^^^^^^^^^^^^^^ required by this bound in `Fixed::<T>::new`

error[E0277]: `String` owns a heap allocation, so it cannot be the inner type of a `Fixed`
  --> tests/compile-fail/fixed_reallocating_inner.rs:24:24
   |
24 |     let _ = Fixed::new(String::from("secret"));
   |             ---------- ^^^^^^^^^^^^^^^^^^^^^^ `Fixed` holds its secret inline; `String` does not qualify
   |             |
   |             required by a bound introduced by this call
   |
   = help: the trait `FixedStorage` is not implemented for `String`
   = note: `Fixed<T>` exists so a secret can be held with no allocator at all, and `FixedStorage` is what keeps that true: a heap allocation can be abandoned unwiped, either by a reallocation or by replacing the whole value.
   = note: For a heap-backed secret use `Dynamic<T>` instead, which documents the residue and offers one safe growth path. `Dynamic` needs this crate's `alloc` feature — if you are seeing this with `alloc` off while still able to name `Vec` or `String`, enabling it is the fix.
   = note: For a custom inner type that genuinely owns no heap allocation, write an empty `impl FixedStorage` block for it. That is an assertion the compiler cannot check, so only write it if it is true.
   = help: the following other types implement trait `FixedStorage`:
             ()
             (A, B)
             (A, B, C)
             (A, B, C, D)
             (A,)
             Option<T>
             [T; N]
             bool
           and $N others
note: required by a bound in `Fixed::<T>::new`
  --> src/fixed.rs
   |
   |     pub const fn new(value: T) -> Self
   |                  --- required by a bound in this associated function
   |     where
   |         T: crate::FixedStorage,
   |            ^^^^^^^^^^^^^^^^^^^ required by this bound in `Fixed::<T>::new`

error[E0277]: `Vec<u8>` owns a heap allocation, so it cannot be the inner type of a `Fixed`
  --> tests/compile-fail/fixed_reallocating_inner.rs:25:24
   |
25 |     let _ = Fixed::new([vec![1u8], vec![2u8]]);
   |             ---------- ^^^^^^^^^^^^^^^^^^^^^^ `Fixed` holds its secret inline; `Vec<u8>` does not qualify
   |             |
   |             required by a bound introduced by this call
   |
   = help: the trait `FixedStorage` is not implemented for `Vec<u8>`
   = note: `Fixed<T>` exists so a secret can be held with no allocator at all, and `FixedStorage` is what keeps that true: a heap allocation can be abandoned unwiped, either by a reallocation or by replacing the whole value.
   = note: For a heap-backed secret use `Dynamic<T>` instead, which documents the residue and offers one safe growth path. `Dynamic` needs this crate's `alloc` feature — if you are seeing this with `alloc` off while still able to name `Vec` or `String`, enabling it is the fix.
   = note: For a custom inner type that genuinely owns no heap allocation, write an empty `impl FixedStorage` block for it. That is an assertion the compiler cannot check, so only write it if it is true.
   = help: the following other types implement trait `FixedStorage`:
             ()
             (A, B)
             (A, B, C)
             (A, B, C, D)
             (A,)
             Option<T>
             [T; N]
             bool
           and $N others
   = note: required for `[Vec<u8>; 2]` to implement `FixedStorage`
note: required by a bound in `Fixed::<T>::new`
  --> src/fixed.rs
   |
   |     pub const fn new(value: T) -> Self
   |                  --- required by a bound in this associated function
   |     where
   |         T: crate::FixedStorage,
   |            ^^^^^^^^^^^^^^^^^^^ required by this bound in `Fixed::<T>::new`

error[E0277]: `Vec<u8>` owns a heap allocation, so it cannot be the inner type of a `Fixed`
  --> tests/compile-fail/fixed_reallocating_inner.rs:26:29
   |
26 |     let _ = Fixed::new(Some(vec![1u8]));
   |             ----------      ^^^^^^^^^ `Fixed` holds its secret inline; `Vec<u8>` does not qualify
   |             |
   |             required by a bound introduced by this call
   |
   = help: the trait `FixedStorage` is not implemented for `Vec<u8>`
   = note: `Fixed<T>` exists so a secret can be held with no allocator at all, and `FixedStorage` is what keeps that true: a heap allocation can be abandoned unwiped, either by a reallocation or by replacing the whole value.
   = note: For a heap-backed secret use `Dynamic<T>` instead, which documents the residue and offers one safe growth path. `Dynamic` needs this crate's `alloc` feature — if you are seeing this with `alloc` off while still able to name `Vec` or `String`, enabling it is the fix.
   = note: For a custom inner type that genuinely owns no heap allocation, write an empty `impl FixedStorage` block for it. That is an assertion the compiler cannot check, so only write it if it is true.
   = help: the following other types implement trait `FixedStorage`:
             ()
             (A, B)
             (A, B, C)
             (A, B, C, D)
             (A,)
             Option<T>
             [T; N]
             bool
           and $N others
   = note: required for `Option<Vec<u8>>` to implement `FixedStorage`
note: required by a bound in `Fixed::<T>::new`
  --> src/fixed.rs
   |
   |     pub const fn new(value: T) -> Self
   |                  --- required by a bound in this associated function
   |     where
   |         T: crate::FixedStorage,
   |            ^^^^^^^^^^^^^^^^^^^ required by this bound in `Fixed::<T>::new`

error[E0277]: `Vec<u8>` owns a heap allocation, so it cannot be the inner type of a `Fixed`
  --> tests/compile-fail/fixed_reallocating_inner.rs:27:36
   |
27 |     let _ = Fixed::new(([0u8; 32], vec![1u8]));
   |             ----------             ^^^^^^^^^ `Fixed` holds its secret inline; `Vec<u8>` does not qualify
   |             |
   |             required by a bound introduced by this call
   |
   = help: the trait `FixedStorage` is not implemented for `Vec<u8>`
   = note: `Fixed<T>` exists so a secret can be held with no allocator at all, and `FixedStorage` is what keeps that true: a heap allocation can be abandoned unwiped, either by a reallocation or by replacing the whole value.
   = note: For a heap-backed secret use `Dynamic<T>` instead, which documents the residue and offers one safe growth path. `Dynamic` needs this crate's `alloc` feature — if you are seeing this with `alloc` off while still able to name `Vec` or `String`, enabling it is the fix.
   = note: For a custom inner type that genuinely owns no heap allocation, write an empty `impl FixedStorage` block for it. That is an assertion the compiler cannot check, so only write it if it is true.
   = help: the following other types implement trait `FixedStorage`:
             ()
             (A, B)
             (A, B, C)
             (A, B, C, D)
             (A,)
             Option<T>
             [T; N]
             bool
           and $N others
   = note: required for `([u8; 32], Vec<u8>)` to implement `FixedStorage`
note: required by a bound in `Fixed::<T>::new`
  --> src/fixed.rs
   |
   |     pub const fn new(value: T) -> Self
   |                  --- required by a bound in this associated function
   |     where
   |         T: crate::FixedStorage,
   |            ^^^^^^^^^^^^^^^^^^^ required by this bound in `Fixed::<T>::new`

error[E0277]: `Box<[u8]>` owns a heap allocation, so it cannot be the inner type of a `Fixed`
  --> tests/compile-fail/fixed_reallocating_inner.rs:34:24
   |
34 |     let _ = Fixed::new(boxed);
   |             ---------- ^^^^^ `Fixed` holds its secret inline; `Box<[u8]>` does not qualify
   |             |
   |             required by a bound introduced by this call
   |
   = help: the trait `FixedStorage` is not implemented for `Box<[u8]>`
   = note: `Fixed<T>` exists so a secret can be held with no allocator at all, and `FixedStorage` is what keeps that true: a heap allocation can be abandoned unwiped, either by a reallocation or by replacing the whole value.
   = note: For a heap-backed secret use `Dynamic<T>` instead, which documents the residue and offers one safe growth path. `Dynamic` needs this crate's `alloc` feature — if you are seeing this with `alloc` off while still able to name `Vec` or `String`, enabling it is the fix.
   = note: For a custom inner type that genuinely owns no heap allocation, write an empty `impl FixedStorage` block for it. That is an assertion the compiler cannot check, so only write it if it is true.
   = help: the following other types implement trait `FixedStorage`:
             ()
             (A, B)
             (A, B, C)
             (A, B, C, D)
             (A,)
             Option<T>
             [T; N]
             bool
           and $N others
note: required by a bound in `Fixed::<T>::new`
  --> src/fixed.rs
   |
   |     pub const fn new(value: T) -> Self
   |                  --- required by a bound in this associated function
   |     where
   |         T: crate::FixedStorage,
   |            ^^^^^^^^^^^^^^^^^^^ required by this bound in `Fixed::<T>::new`

error[E0277]: `Box<[u8]>` owns a heap allocation, so it cannot be the inner type of a `Fixed`
  --> tests/compile-fail/fixed_reallocating_inner.rs:35:29
   |
35 |     let _ = Fixed::new(Some(vec![1u8].into_boxed_slice()));
   |             ----------      ^^^^^^^^^^^^^^^^^^^^^^^^^^^^ `Fixed` holds its secret inline; `Box<[u8]>` does not qualify
   |             |
   |             required by a bound introduced by this call
   |
   = help: the trait `FixedStorage` is not implemented for `Box<[u8]>`
   = note: `Fixed<T>` exists so a secret can be held with no allocator at all, and `FixedStorage` is what keeps that true: a heap allocation can be abandoned unwiped, either by a reallocation or by replacing the whole value.
   = note: For a heap-backed secret use `Dynamic<T>` instead, which documents the residue and offers one safe growth path. `Dynamic` needs this crate's `alloc` feature — if you are seeing this with `alloc` off while still able to name `Vec` or `String`, enabling it is the fix.
   = note: For a custom inner type that genuinely owns no heap allocation, write an empty `impl FixedStorage` block for it. That is an assertion the compiler cannot check, so only write it if it is true.
   = help: the following other types implement trait `FixedStorage`:
             ()
             (A, B)
             (A, B, C)
             (A, B, C, D)
             (A,)
             Option<T>
             [T; N]
             bool
           and $N others
   = note: required for `Option<Box<[u8]>>` to implement `FixedStorage`
note: required by a bound in `Fixed::<T>::new`
  --> src/fixed.rs
   |
   |     pub const fn new(value: T) -> Self
   |                  --- required by a bound in this associated function
   |     where
   |         T: crate::FixedStorage,
   |            ^^^^^^^^^^^^^^^^^^^ required by this bound in `Fixed::<T>::new`

error[E0277]: `Box<[u8]>` owns a heap allocation, so it cannot be the inner type of a `Fixed`
  --> tests/compile-fail/fixed_reallocating_inner.rs:36:24
   |
36 |     let _ = Fixed::new([vec![1u8].into_boxed_slice(), vec![2u8].into_boxed_slice()]);
   |             ---------- ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ `Fixed` holds its secret inline; `Box<[u8]>` does not qualify
   |             |
   |             required by a bound introduced by this call
   |
   = help: the trait `FixedStorage` is not implemented for `Box<[u8]>`
   = note: `Fixed<T>` exists so a secret can be held with no allocator at all, and `FixedStorage` is what keeps that true: a heap allocation can be abandoned unwiped, either by a reallocation or by replacing the whole value.
   = note: For a heap-backed secret use `Dynamic<T>` instead, which documents the residue and offers one safe growth path. `Dynamic` needs this crate's `alloc` feature — if you are seeing this with `alloc` off while still able to name `Vec` or `String`, enabling it is the fix.
   = note: For a custom inner type that genuinely owns no heap allocation, write an empty `impl FixedStorage` block for it. That is an assertion the compiler cannot check, so only write it if it is true.
   = help: the following other types implement trait `FixedStorage`:
             ()
             (A, B)
             (A, B, C)
             (A, B, C, D)
             (A,)
             Option<T>
             [T; N]
             bool
           and $N others
   = note: required for `[Box<[u8]>; 2]` to implement `FixedStorage`
note: required by a bound in `Fixed::<T>::new`
  --> src/fixed.rs
   |
   |     pub const fn new(value: T) -> Self
   |                  --- required by a bound in this associated function
   |     where
   |         T: crate::FixedStorage,
   |            ^^^^^^^^^^^^^^^^^^^ required by this bound in `Fixed::<T>::new`
