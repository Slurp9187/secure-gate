[package]
name = "evasion-corpus"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
secure-gate = { path = "../../..", features = ["std", "serde", "cloneable"] }
zeroize = { version = "1.8", default-features = false }
serde_json = "1.0"
helpers = { path = "../helpers" }

[workspace]
